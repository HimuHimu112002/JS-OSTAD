function minNum(x1,x2,x3) {
    // let numArr = input.trim().split(/\s+/).map(Number);
    // let x1 = numArr[0];
    // let x2 = numArr[1];
    // let x3 = numArr[2];
    console.log(Math.min(x1,x2,x3))
}
minNum(10,20,30)